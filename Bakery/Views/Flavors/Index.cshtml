@{
  Layout = "_Layout";
}
@using Bakery.Models

<h1>Flavors</h1>
<!-- List all flavors -->
<ul class="list-group">
  @if (Model.Count != 0)
  {
    foreach (Flavor flav in Model)
    {
      <!-- Link to Details -->
      <li class="list-group-item list-group-item-action">@Html.ActionLink($"{flav.Type}", "Details", new {id = flav.FlavorId})</li>
    }
  }
<ul>
  <br>
  <!-- Link to Create new flavor -->
  <button>@Html.ActionLink("Add Flavor", "Create")</button>



